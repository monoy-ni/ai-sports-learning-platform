# 状态管理

`0.1.0` 使用轻量 React 状态即可：

- 登录 token 和当前用户信息放在 Auth Provider。
- 服务端数据通过 API 函数获取。
- 表单使用组件本地状态。
- 看板筛选条件先不做全局状态，等出现明显复用后再抽象。

如果后续请求缓存和刷新逻辑变复杂，可以在 `shared/api` 后面引入 TanStack Query，不改变页面层调用方式。
