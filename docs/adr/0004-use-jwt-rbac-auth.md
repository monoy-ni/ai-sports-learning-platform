# ADR-0004：使用 JWT + RBAC

## 状态

已接受

## 背景

一期需要学生和教师访问控制，但暂不接入学校统一身份认证。

## 决策

使用 JWT 访问令牌和基于角色的权限控制，角色包括 `STUDENT`、`TEACHER` 和 `ADMIN`。

## 影响

产品可以先用简单账号完成本地和线上验证。未来可在 Auth Service 后面增加 SSO 适配层。
